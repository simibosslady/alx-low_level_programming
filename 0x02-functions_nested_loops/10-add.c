#include "main.h"
#include <stdio.h>
/**
 * add -To add two integers and return the result
 * @a:the first number
 * @b: the second number
 * Return: integer value
 */

int add(int a, int b)
{
	return (a + b);
}
